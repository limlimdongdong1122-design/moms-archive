# 코어 실행에는 표준 라이브러리만 필요합니다 (런타임 의존성 0).
# 아래는 '암호화 볼트' 기능을 사용할 때만 필요한 선택적 의존성입니다.
cryptography>=3.4

# 빌드/개발용(실행에는 불필요):
#   pyinstaller   # 단일 실행 파일 빌드
#   pillow        # PWA 아이콘 생성(tools/make_icons.py)
